# Audit exclusions for `tama find-violations`: generated output and paths
# fixed by something outside this project.
#
# Written by the wisent-visuals banner bot. The banner below is rendered
# from .github/banner.toml and replaced whole on every run, so a repository
# audit reads it as generated output instead of asking somebody to split it.
assets/readme-banner.svg
# The repository root. Every file here is read by a tool at exactly this
# path: SwiftPM reads Package.swift and resolves Package.resolved beside it,
# the release tooling reads .wisent-release.json, consumers fetch
# release-manifest.schema.json by its published path, git reads .gitignore,
# and GitHub reads README.md and LICENSE.
.
